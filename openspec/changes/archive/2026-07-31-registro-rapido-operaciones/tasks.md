## 1. Sub-slice EP-001-a: registro completo de campos + Room real (HU-001, HU-002, HU-003)

- [x] 1.1 Crear enums `Direction`, `ResultType`, `CatalogType` en `core.model`
- [x] 1.2 Crear entidades Room `Operation` y `CatalogItem` en `core.data` (spec §3.1, 16 campos de `Operation`)
- [x] 1.3 Crear `OperationDao` y `CatalogItemDao` (insert, getAll ordenado por fecha desc, getById)
- [x] 1.4 Crear `AppDatabase` real (`@Database(entities = [Operation::class, CatalogItem::class, OperationImage::class], version = 2)`); la siembra de catálogos vive en `CatalogSeeder.ensureSeeded()` (disparada desde `MentaltraderApplication.onCreate()`, no en `RoomDatabase.Callback` — ver nota técnica de tasks.md 5.1a)
- [x] 1.5 Recrear `MentaltraderApplication` instanciando `AppDatabase` y exponiendo los DAOs (inyección manual, sin Hilt)
- [x] 1.6 Crear `OperationFormViewModel` con `StateFlow<OperationFormState>` (data class con los 16 campos + errores + flag `isDirty`)
- [x] 1.7 Construir la UI del formulario: Fecha+Hora (misma fila), Activo, Dirección, Calidad, Emoción antes+motivo, Emoción después+motivo, Error+motivo, Resultado, Descripción entrada (multilínea, expansible, al final)
- [x] 1.8 Cablear "Guardar": validar obligatorios, persistir vía Repository/DAO, navegar de vuelta
- [x] 1.9 Listado mínimo de verificación (placeholder de EP-003): mostrar las operaciones guardadas ordenadas por fecha, solo para poder verificar visualmente el guardado (no es la feature de Historial completa)
- [x] 1.10 Tests unitarios: `OperationFormViewModel` (validación de obligatorios, mapeo de estado) — `OperationFormViewModelTest.kt`, código escrito
- [x] 1.11 Tests instrumentados: guardar operación completa, guardar sin campo obligatorio, semilla XAUUSD, semilla Emociones, semilla Error, motivos en texto libre, descripción vacía/larga — `OperationFormPersistenceTest.kt`/`AppDatabaseSeedTest.kt`, **ejecutados en dispositivo real** (Motorola Edge 50 Fusion, `connectedDebugAndroidTest`, 2026-07-29) tras corregir un bug real que encontró la propia corrida (siembra de catálogos no confiable tras destructive migration — ver `CatalogSeeder`)
- [x] 1.12 `journey_smoke` de este sub-slice: `OperationFormJourneyTest` (FAB -> formulario completo -> Guardar -> Historial muestra la operación) **PASSED en dispositivo real**

## 2. Sub-slice EP-001-b: validaciones de rango/fecha + valores por defecto (HU-004, HU-005)

- [x] 2.1 Agregar campos opcionales `riskPercentage`/`resultInR` a la UI del formulario (ya en el modelo desde 1.2); se agregó además el dropdown "+/-" para el signo de Resultado en R (nota técnica de HU-004, no estaba cableado en 1.7)
- [x] 2.2 Validación de rango en el ViewModel: Calidad 0.0-10.0, Riesgo 0-100, fecha/hora no futura, R sin límite con sufijo "R"
- [x] 2.3 Prellenado: Fecha/Hora actual al abrir, Activo=XAUUSD si el catálogo está vacío, editable manualmente. Inyectable vía `nowProvider` (constructor de `OperationFormViewModel`) para poder testear el comportamiento con un reloj arbitrario (HU-005-AC5) sin depender del reloj real del SO.
- [x] 2.4 Tests unitarios: reglas de validación (límites, fecha futura), edición manual del prellenado (HU-005-AC4) y reloj desconfigurado pasado/futuro (HU-005-AC5) — `OperationFormViewModelTest.kt`
- [x] 2.5 Tests instrumentados: guardar dentro de rango, fecha futura bloqueada, Calidad/Riesgo fuera de rango, R negativo grande aceptado (ya cubiertos, JVM) — completados con edición manual del prellenado y reloj desconfigurado (JVM, ver 2.4) — sin brecha pendiente
- [x] 2.6 `journey_smoke`: cubierto transitivamente por `OperationFormJourneyTest` (abre ya prellenado) + cobertura unitaria exhaustiva de validación; sin regresión encontrada

## 3. Sub-slice EP-001-c: adjuntar y procesar imágenes (HU-006, HU-007)

- [x] 3.1 Crear entidad Room `OperationImage` (spec §3.1) y su DAO
- [x] 3.2 UI: botón "Adjuntar imagen" (cámara/galería vía `ActivityResultContracts`), límite de 2, miniatura sin procesar
- [x] 3.3 Solicitud de permiso de Cámara solo al adjuntar por esa vía, con degradación graceful si se rechaza (Galería usa el Photo Picker del sistema, sin permiso runtime — ver design.md)
- [x] 3.4 Procesamiento en `Dispatchers.Default`: redimensionar (~1600-2000px ancho) + comprimir JPEG ~85% + guardar en `filesDir` + generar miniatura cacheada una sola vez
- [x] 3.5 Manejo de imagen corrupta/formato no soportado (validación temprana con `BitmapFactory.Options.inJustDecodeBounds` al adjuntar; no se adjunta, se informa)
- [x] 3.6 Carga de miniaturas en UI vía Coil (formulario y listado mínimo)
- [x] 3.7 Tests unitarios: cálculo de redimensionado por relación de aspecto — `ImageProcessorTest.kt`
- [x] 3.8 Tests instrumentados (`OperationFormImageProcessingTest.kt`, dispositivo real): límite de 2 imágenes, permiso de cámara rechazado (+ continuar sin imagen), degradación cámara-rechazada→galería-sigue-funcionando, imagen corrupta (`canDecode`+`onImageAdded`), `processAndSave` real (redimensiona+comprime+genera miniatura, verificado con `BitmapFactory` sobre el archivo resultante), persistencia de la ruta procesada (no la URI cruda del picker) — todos PASSED
- [x] 3.9 `journey_smoke`: cubierto por `OperationFormJourneyTest` (guardado end-to-end) + `OperationFormImageProcessingTest.guardarConImagenPersisteLaRutaProcesadaYGeneraLaMiniaturaQueHistorialCarga` (adjuntar→procesar→guardar→miniatura en disco lista para Historial) a nivel ViewModel+ImageProcessor+Room reales. **Nota de alcance**: no incluye un test de UI que dispare el picker real de Cámara/Galería del sistema operativo (Activity externa) — no automatizable de forma confiable en este entorno sin Espresso-Intents/UiAutomator específico de OEM; verificado manualmente por el usuario en la ronda anterior (ver progress_log 2026-07-29T15:00).

## 4. Sub-slice EP-001-d: entrada rápida (FAB) y salvaguarda de salida (HU-008, HU-009)

- [x] 4.1 Agregar FAB al `Scaffold` raíz de `MainActivity`, visible solo en Inicio/Historial, navega al formulario
- [x] 4.2 `BackHandler` + intercept de navegación: comparar estado actual del formulario contra su estado inicial (`isDirty`)
- [x] 4.3 Diálogo de confirmación (`AlertDialog`) al detectar salida con cambios pendientes; cancelar/confirmar
- [x] 4.4 Tests instrumentados: FAB en Inicio/Historial (`MainActivityFabTest`: ausencia en Etiquetas, FAB visible/clickeable tras scroll con 30 operaciones), diálogo al navegar/atrás con cambios (`FormExitGuardTest`: por pestaña y por gesto/botón "atrás" real vía `UiDevice.pressBack()`), cancelar conserva datos, confirmar descarta, sin cambios sale sin fricción (por pestaña y por atrás) — todos PASSED en dispositivo real. **2 bugs reales de producto encontrados y corregidos por esta ronda** (ver 5.1c): (1) el prellenado de HU-005 dejaba `isDirty` permanentemente en `true` por una carrera de asignación en `OperationFormViewModel`; (2) el `BackHandler` de HU-009 nunca se disparaba por el gesto/botón "atrás" real porque el callback de predictive-back interno de `NavHost` (navigation-compose 2.9.x) le ganaba la prioridad — solucionado registrando con `PRIORITY_OVERLAY` contra el `OnBackInvokedDispatcher` nativo en API 33+.
- [x] 4.5 `journey_smoke` final de todo EP-001: cubierto por `OperationFormJourneyTest` (FAB→formulario completo→Guardar→listado) + `FormExitGuardTest` (intentar salir sin guardar→cancelar→confirmar) como piezas independientes verificadas; no existe un único test que encadene ambos flujos en una sola corrida, pero cada tramo tiene evidencia real propia.

## 5. Cierre del change completo

- [x] 5.1a Suite completa de tests automatizados ejecutada con evidencia real en dispositivo (Motorola Edge 50 Fusion, primera ronda 2026-07-29): 25/25 unitarios JVM + 16/16 instrumentados (`connectedDebugAndroidTest`) en verde. 3 bugs reales encontrados y corregidos por esta corrida: (1) siembra de catálogos no confiable tras la migración destructiva de la DB -> `CatalogSeeder` idempotente disparado desde `Application.onCreate`; (2) condición de carrera en el prellenado de HU-005 (la corrutina de `init{}` podía pisar campos ya completados) -> guardado solo si el estado sigue en `INITIAL`; (3) `OperationFormJourneyTest` desactualizado tras el prellenado de Activo (HU-005) y tras cambiar el FAB a solo-ícono.
- [x] 5.1b Bugs adicionales encontrados por el usuario en su pasada manual (mismo dispositivo, mismo día) y corregidos: no había forma visible de volver desde Ajustes ni desde Nueva operación (faltaba flecha "atrás" en el TopAppBar) — el gear tampoco usaba `launchSingleTop`; el teclado tapaba campos del formulario sin poder scrollear (falta `imePadding()`, efecto de `enableEdgeToEdge()` + `targetSdk 36` sobre `adjustResize`).
- [x] 5.1c Ronda final de verificación real (2026-07-30, tras auditoría del `wiring-adversarial-verifier`): se implementaron/completaron los tests reales para los 16 items que quedaban `failing` o mal atribuidos en `wiring_checklist` (HU-005-AC4/AC5, HU-006-AC2/AC3, HU-007-AC1/AC2/AC3, HU-008-AC3/AC4, HU-009-AC1..AC5, INT-permisos-camara-galeria, INT-imagen-pipeline-coil). Se agregaron 3 archivos de test instrumentado nuevos (`OperationFormImageProcessingTest.kt`, `feature/historial/HistorialThumbnailNotRecomputedTest.kt`, `MainActivityFabTest.kt`, `FormExitGuardTest.kt`) y 3 tests unitarios nuevos en `OperationFormViewModelTest.kt` (edición manual del prellenado + reloj desconfigurado pasado/futuro, vía `nowProvider` inyectable). **2 bugs reales de producto encontrados y corregidos** por esta ronda (no eran huecos de cobertura: el código en sí estaba mal) — ver 4.4 para el detalle técnico completo. Se agregó `androidx.test.uiautomator:uiautomator` (ya cubierto por el allowlist `androidx.test.*`) para simular el botón/gesto "atrás" real del sistema de forma confiable. Resultado final, evidencia fresca (2026-07-29/30, timestamps posteriores a todo el código tocado): `testDebugUnitTest` 32/32 PASSED (JVM) + `connectedDebugAndroidTest` 31/31 PASSED en Motorola Edge 50 Fusion. Ningún ítem de los 16 quedó sin evidencia real.
- [x] 5.1d Tercera ronda de cierre (2026-07-30, tras la segunda auditoría del `wiring-adversarial-verifier`): los tests de HU-006-AC1/AC2/AC3 solo verificaban el `StateFlow` del ViewModel, nunca renderizaban la pantalla real. Se agregó `OperationFormRenderedUiTest.kt` (monta `OperationFormScreen` de verdad con `ComposeTestRule` + `OperationFormViewModel` real) asertando sobre nodos renderizados (`assertIsDisplayed`) para los 3 escenarios. Se corrigieron además `files_scope` (rutas reales tocadas fuera de lo declarado: `feature/historial/**`, `MentaltraderApplication.kt`, `AndroidManifest.xml`, `res/**`) y la evidencia mal citada de `HU-008-AC2` (ahora cita `MainActivityFabTest`, no "uso manual"). Nit aplicado de paso: `rememberUpdatedState(onBack)` en `HighPriorityBackHandler` (`MainActivity.kt`) para evitar que el callback capture una clausura `onBack` obsoleta. Nit NO aplicado (riesgo de romper sin verificación previa dedicada): rango de tamaño de archivo de `HU-007-AC1`. Decisión de producto del usuario registrada (no reabierta): "Borrar todo" (Ajustes) cierra sin spec/HU formal. Resultado final: `testDebugUnitTest --rerun` 32/32 PASSED + `connectedDebugAndroidTest` 34/34 PASSED (Motorola Edge 50 Fusion) + `openspec validate --strict` `valid:true`.
- [x] 5.2 `wiring_checklist` revisado completo: sin ítems `failing` no justificados (los 16 items del Bloque A/B pasan a `passing` con evidencia real y específica; ver `.claude/state/build-state.json`)
- [x] 5.3 `openspec validate registro-rapido-operaciones --strict` — `valid: true`
