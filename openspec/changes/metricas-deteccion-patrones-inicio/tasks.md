## 1. Sub-slice EP-004-a: núcleo de métricas (resumen numérico, gráficas) + ranking de emociones/errores (HU-026, HU-027)

- [x] 1.1 `OperationDao.metricsSummary()`: agregación en SQL de todas las operaciones (conteo, % ganadas/perdidas/BE, calidad promedio, R total, R promedio) -- `CASE`/`COALESCE` evitan división por cero y `NULL` con cero operaciones (HU-026 Escenario 4). `design.md` decisión #2: sin `dateFrom`/`dateTo` todavía (EP-004-a asume "todas las operaciones", el selector real llega en EP-004-b).
- [x] 1.2 `OperationMetricsSummary` (`core/data`, nuevo data class) -- transporta lo ya calculado en SQL, sin recalcular nada (mismo criterio que `MonthSummary`).
- [x] 1.3 `OperationDao.resultInROrderedByDateAsc()`: proyección liviana de una sola columna (`Flow<List<Float?>>`), ordenada cronológicamente ascendente -- NO la entidad `Operation` completa (design.md decisión #3: SQLite de minSdk 27 no garantiza funciones de ventana, el punto-a-punto se resuelve en Kotlin puro, no en SQL).
- [x] 1.4 `ResultInRCumulativeSeries` (`feature/inicio/`, función pura): transforma la lista ordenada de `resultInR` en los puntos punto-a-punto del R acumulado (HU-026 Escenario 2), tratando `null` como `0` sin romper la serie (Escenario 4).
- [x] 1.5 `OperationDao.emotionRanking()`/`errorRanking()`: `GROUP BY`/`JOIN` en SQL contra `catalog_item`, desempate por nombre ascendente (HU-027 Escenario 3). `emotionRanking` cuenta AMBOS roles (`emotionBeforeId`/`emotionAfterId`) vía `UNION ALL` antes de agrupar (design.md decisión #4). `CatalogRankingItem` (`core/data`, nuevo data class) transporta id/nombre/frecuencia ya resueltos.
- [x] 1.6 `InicioViewModel` nuevo (`feature/inicio/`): expone `metricsSummary`/`emotionRanking`/`errorRanking` como pass-through directos de `OperationDao` (mismo criterio que `HistorialViewModel.monthSummaries`/`assets`, envueltos en `flow { emitAll(...) }`) y `cumulativeSeries` (única transformación real del ViewModel, vía `ResultInRCumulativeSeries.build`).
- [x] 1.7 `InicioScreen` real (`feature/inicio/`, reemplaza el placeholder movido desde `feature/metricas/`): tarjetas de resumen numérico, gráfica de línea de R acumulado y gráfica de barras de distribución (`InicioCharts.kt`, Compose Canvas puro -- design.md decisión #1, sin librería nueva), y las 2 secciones de ranking (emociones, errores). Colores verde/rojo para valores con signo (`InicioFormatting.colorFor`), formateo determinístico con `Locale.US` (mismo criterio que `MonthSummaryFormatting`).
- [x] 1.8 Cableado real: `MainActivity.kt` reemplaza `InicioScreen()` (placeholder) por `InicioViewModel`/`InicioScreen` reales, vía `InicioViewModel.Factory(application.database.operationDao())` -- misma ruta `Destino.Inicio.route` ya existente (EP-005), sin cambios de navegación.
- [x] 1.9 Tests unitarios: `ResultInRCumulativeSeriesTest` (6 tests: vacío, un valor, varios valores acumulando en orden, valor `null` no rompe la serie, todos `null` da serie plana en 0, acumulado puede terminar negativo) + `InicioViewModelTest` (3 tests: sin operaciones la serie queda vacía, la serie refleja el R de cada operación en orden cronológico real -- no el de inserción --, un valor `null` no rompe la serie) -- `testDebugUnitTest` (176 tests en toda la suite) + `assembleDebug` + `compileDebugAndroidTestKotlin`: BUILD SUCCESSFUL, sin regresiones. `metricsSummary`/`emotionRanking`/`errorRanking` NO se ejercitan a nivel de `InicioViewModel` (agregación SQL real, mismo criterio ya establecido para `HistorialViewModel.monthSummaries` -- diferido al instrumentado de 1.10).
- [ ] 1.10 Test instrumentado con Room real: verificar `metricsSummary`/`emotionRanking`/`errorRanking` contra un dataset sembrado (incluyendo el caso de cero operaciones y el de una operación con `resultInR = null`, para confirmar el comportamiento real del warning de KSP documentado en `design.md` Riesgos) -- diferido a la pasada final de instrumentados (sin dispositivo conectado en esta sesión, mismo criterio ya usado en EP-003/EP-005).
- [ ] 1.11 `journey_smoke` EP-004-a: abrir Inicio con operaciones reales de distintos resultados/emociones/errores y ver las tarjetas, gráficas y rankings reales -- diferido a la pasada final de instrumentados, misma razón que 1.10.

## 2. Sub-slice EP-004-b: selector de periodo predefinido (HU-028)

- [x] 2.1 Extendidas `OperationDao.metricsSummary()`/`resultInROrderedByDateAsc()`/`emotionRanking()`/`errorRanking()` con `dateFrom`/`dateTo` (`Long?`, no default -- mismo criterio explícito ya usado en `pagingSourceFiltered`, sin depender de valores por defecto en un método abstracto de un `@Dao`) -- patrón `(:param IS NULL OR ...)` idéntico al de `pagingSourceFiltered` (EP-003); con ambos `null` siguen devolviendo exactamente "todas las operaciones" (mismo comportamiento que EP-004-a). Se optó por AGREGAR los parámetros a la MISMA función (no una sobrecarga separada), documentado en el KDoc de `OperationDao.metricsSummary`. `emotionRanking` aplica el filtro DENTRO de cada rama del `UNION ALL` (misma operación, mismo `dateTime`, antes de combinar los 2 roles).
- [x] 2.2 `PeriodoInicioFiltro`/`PeriodoInicioFiltroRange` (`feature/inicio/InicioFilterState.kt`, función pura, análoga a `PeriodoFiltro`/`PeriodoFiltroRange` de `feature/historial` pero un enum SEPARADO -- design.md decisión #7) para Día/Semana/Mes/Últimos 3 meses. A diferencia de `PeriodoFiltroRange.rangeFor` (nullable, por `PERSONALIZADO`), `PeriodoInicioFiltroRange.rangeFor` siempre devuelve un rango no-nulo (no existe `PERSONALIZADO` en este enum todavía, ver design.md decisión #7).
- [x] 2.3 `InicioViewModel` gana `filterState` (`StateFlow<InicioFilterState>`) y `onSelectPeriodo(periodo, today)`; `metricsSummary`/`cumulativeSeries`/`emotionRanking`/`errorRanking` se reconstruyen vía `flatMapLatest` sobre `filterState` (HU-028 Escenario 2: `flatMapLatest` cancela la colección anterior antes de suscribirse a la nueva -- recalcula desde cero, sin arrastrar datos del periodo anterior, verificado con test real, ver 2.5).
- [x] 2.4 `PeriodoSelectorRow` (chips Día/Semana/Mes/Últimos 3 meses) en `InicioScreen`, como hermano del `LazyColumn` dentro del `Column` raíz (NO como `item` del `LazyColumn`) -- HU-028 Escenario 3: permanece fijo y visible al hacer scroll, garantía ESTRUCTURAL (el selector queda fuera del área que se desplaza), pendiente de confirmación visual real en el instrumentado (2.6).
- [x] 2.5 Tests unitarios (JVM, sin Room): `PeriodoInicioFiltroRangeTest` (5 tests: Día/Semana/Mes/Últimos 3 meses + invariante "todos terminan hoy") + `InicioViewModelTest` (+5 tests: filterState por defecto, `onSelectPeriodo` resuelve dateFrom/dateTo reales, recalcular filtrando solo el rango, cambiar de periodo sin arrastrar datos del anterior, deseleccionar vuelve a "todas") -- `testDebugUnitTest` (186 tests) + `assembleDebug` + `compileDebugAndroidTestKotlin`: BUILD SUCCESSFUL, sin regresiones. Bug real encontrado y corregido durante el TDD: los primeros 2 tests de recálculo fallaron porque construían los timestamps de prueba con zona UTC explícita, mientras `onSelectPeriodo`/`PeriodoInicioFiltroRange.rangeFor` resuelven con `ZoneId.systemDefault()` (sin zona explícita) -- corregido usando `ZoneId.systemDefault()` también en el test, para no depender de una coincidencia del huso horario de la máquina.
- [ ] 2.6 Test instrumentado + `journey_smoke` EP-004-b: seleccionar cada periodo predefinido real y verificar el recálculo, y confirmar visualmente que el selector permanece fijo al hacer scroll -- diferido a la pasada final de instrumentados (sin dispositivo conectado en esta sesión, mismo criterio ya usado en EP-004-a/EP-003/EP-005).

## 3. Sub-slice EP-004-c: estado vacío en Inicio (HU-031)

- [x] 3.1 `InicioViewModel` expone `totalOperationCount: Flow<Int>` (pass-through directo de `OperationDao.countAll()`, ya existente desde EP-003, mismo criterio que `HistorialViewModel.totalOperationCount`) -- conteo GLOBAL, sin filtrar por periodo, para distinguir "primera vez" de "sin datos para el periodo". `InicioEmptyState` (`feature/inicio/InicioEmptyState.kt`, enum + función pura `resolve(totalOperationCount, operationCountInPeriod)`, sin Room/Compose, mismo patrón que `HistorialEmptyState`/HU-025) combina esa señal con `metricsSummary.operationCount` (ya calculado en SQL, HU-026-AC1) SIN ninguna query nueva.
- [x] 3.2 `InicioScreen` reutiliza `HistorialEmptyState`/`HistorialEmptyStateContent` (EP-003, HU-025) para el caso de "primera vez, nunca registró ninguna operación" (HU-031 Escenario 1) -- `HistorialEmptyStateContent` se hizo público (ya no `private`, `HistorialScreen.kt`) para poder invocarse directamente desde `feature/inicio` con `HistorialEmptyState.PRIMERA_VEZ`, sin duplicar el mensaje/lógica, mismo componente que Historial (design.md decisión #6).
- [x] 3.3 Mensaje propio de "sin datos para el periodo seleccionado" (HU-031 Escenario 2, `InicioEmptyState.SIN_DATOS_PERIODO` -> `InicioEmptyStateContent` en `InicioScreen.kt`), sin acceso directo de creación (las operaciones sí existen, solo no en este rango) -- `PeriodoSelectorRow` sigue visible arriba (fuera del bloque condicional) para poder cambiar de periodo.
- [x] 3.4 Recuperación fluida al cambiar a un periodo con datos (HU-031 Escenario 3): garantía ESTRUCTURAL -- `emptyState` se deriva en cada render 100% de `Flow` reales (`totalOperationCount`/`metricsSummary.operationCount`, vía `collectAsState`), sin ningún estado de UI local que "recordar" del estado vacío anterior; en cuanto `InicioEmptyState.resolve` devuelve `null`, el mismo render muestra el `LazyColumn` de tarjetas/gráficas/ranking normalmente. Confirmación visual real (seleccionar un periodo con datos desde el estado vacío en un dispositivo) pendiente del instrumentado (3.5).
- [x] 3.5 Tests unitarios: `InicioEmptyStateTest` (4 tests: PRIMERA_VEZ sin ninguna operación, SIN_DATOS_PERIODO con operaciones pero ninguna en el periodo, ningún estado vacío con al menos una operación en el periodo, precedencia de PRIMERA_VEZ) + `InicioViewModelTest` (+2 tests: `totalOperationCount` en 0 sin operaciones, `totalOperationCount` refleja el conteo real tras insertar operaciones SIN importar el periodo seleccionado) -- `testDebugUnitTest` (192 tests en toda la suite) + `assembleDebug` + `compileDebugAndroidTestKotlin`: BUILD SUCCESSFUL, sin regresiones. TDD real red->green: ambos archivos de test fallaron en rojo por referencia no resuelta (`InicioEmptyState`, `totalOperationCount`) antes de implementar.
- [ ] 3.6 Test instrumentado + `journey_smoke` EP-004-c: verificar en un dispositivo/emulador real los 3 escenarios (primera vez sin operaciones, periodo sin datos habiendo operaciones en otros periodos, cambiar a un periodo con datos desde el estado vacío) -- diferido a la pasada final de instrumentados (sin dispositivo conectado en esta sesión, mismo criterio ya usado en EP-004-a/EP-004-b/EP-003/EP-005).

## 4. Sub-slice EP-004-d: rango de fechas personalizado + persistencia del filtro de periodo (HU-029, HU-030)

- [x] 4.1 `PeriodoInicioFiltro` gana `PERSONALIZADO` (`InicioFilterState.kt`) -- `PeriodoInicioFiltroRange.rangeFor` cambia su tipo de retorno a `Pair<Long, Long>?` (antes no-nulo, design.md decisión #7 lo prevé explícitamente como evolución de este sub-slice) y devuelve `null` para `PERSONALIZADO`, mismo criterio exacto que `PeriodoFiltroRange.rangeFor` de Historial. Campos de fecha reales "Desde"/"Hasta" (`InicioCustomDateRangeFields`, `InicioScreen.kt`) con el mismo formato `dd/MM/yyyy` de `OperationFormViewModel.DATE_FORMATTER` ya usado por el filtro de Historial (HU-022) -- este proyecto no usa un `DatePicker` nativo en ningún flujo existente (INVEST de HU-029: "el componente exacto de calendario es negociable"). Validación de rango inválido (fin anterior a inicio, HU-029 Escenario 3) real: `InicioViewModel.onCustomDateRange` rechaza el cambio y expone `customRangeError: StateFlow<Boolean>`, que `InicioScreen` traduce en un mensaje visible.
- [x] 4.2 `InicioViewModel.onCustomDateRange(from, to)` (HU-029 Escenario 2) aplica el rango confirmado (`selectedPeriodo = PERSONALIZADO`, `dateFrom`/`dateTo` reales) y dispara el mismo recálculo vía `flatMapLatest` sobre `filterState` que ya usa el periodo predefinido de EP-004-b (sin mecanismo nuevo). `onSelectPeriodo` también se ajustó: al elegir `PERSONALIZADO` preserva `dateFrom`/`dateTo` vigentes (no resuelve rango propio), mismo criterio que `HistorialViewModel.onSelectPeriodo`.
- [x] 4.3 `InicioFilterRepository`/`InicioFilterSnapshot` (`core/data/InicioFilterRepository.kt`, nuevo) -- DataStore Preferences con ARCHIVO ("inicio_filter") y claves propias (`lastInicioPeriodFilter`/`lastInicioCustomRangeStart`/`lastInicioCustomRangeEnd`), independientes de `HistorialFilterRepository` ("historial_filter") -- mismo patrón mecánico que HU-024/EP-003, sin compartir estado ni archivo. `InicioFilterState.toSnapshot()`/`fromSnapshot()` (`InicioFilterState.kt`) traducen hacia/desde el snapshot neutral, mismo criterio que `HistorialFilterState`.
- [x] 4.4 `InicioViewModel` restaura el filtro persistido en `init` (HU-030 Escenario 1/2, vía `InicioFilterState.fromSnapshot`) y persiste en cada cambio real (`onSelectPeriodo`/`onCustomDateRange`, vía `persistFilterState`), mismo patrón que `HistorialViewModel`/HU-024. Decisión de producto documentada (design.md decisión #8, INVEST de HU-030: "el valor por defecto exacto ... es negociable"): sin ningún filtro guardado previamente (HU-030 Escenario 3), el periodo por defecto razonable es **"todas las operaciones"** (`InicioFilterState()`, idéntico al comportamiento ya existente desde EP-004-a/b) -- un periodo predefinido concreto (p.ej. "Mes") podría mostrar "sin datos para el periodo" de entrada a un usuario con historial en otro periodo, un resultado engañoso para un primer arranque tras esta actualización; "todas las operaciones" siempre muestra algo real cuando existe, sin error ni pantalla en blanco, y no reintroduce la ambigüedad (imposible de distinguir en disco, mismo criterio que `HistorialFilterSnapshot`) entre "nunca seleccionó" y "deseleccionó explícitamente".
- [x] 4.5 Tests unitarios: `PeriodoInicioFiltroRangeTest` (+1: `PERSONALIZADO` no tiene rango propio) + `InicioViewModelTest` (+9: HU-029 recalcular/validar rango inválido/limpiar error tras uno inválido, HU-030 persistir tras `onSelectPeriodo`/`onCustomDateRange` válido/inválido, restaurar periodo predefinido/rango personalizado/periodo por defecto sin filtro previo) + `InicioFilterRepositoryTest` (5 tests nuevos, DataStore real sobre archivo temporal, mismo patrón que `HistorialFilterRepositoryTest`, incluyendo el ciclo completo guardar + instancia nueva simula reinicio de proceso) -- `testDebugUnitTest` (207 tests en toda la suite) + `assembleDebug` + `compileDebugAndroidTestKotlin`: BUILD SUCCESSFUL, sin regresiones. TDD real red→green: los 15 tests nuevos fallaron primero por referencia no resuelta (`PERSONALIZADO`, `InicioFilterRepository`, `customRangeError`, `onCustomDateRange`) antes de implementar.
- [ ] 4.6 Test instrumentado + `journey_smoke` EP-004-d: elegir "Personalizado" y confirmar un rango real, confirmar el bloqueo visual de un rango inválido, y verificar la persistencia real entre reinicios COMPLETOS del proceso (no solo en memoria) -- diferido a la pasada final de instrumentados (sin dispositivo conectado en esta sesión, mismo criterio ya usado en EP-004-a/EP-004-b/EP-004-c/EP-003/EP-005).

## 5. Cierre del change completo

- [ ] 5.1 Suite completa (`testDebugUnitTest` + `connectedDebugAndroidTest`) en verde con evidencia real de ejecución, sin regresiones sobre EP-001/EP-002/EP-003/EP-005.
- [ ] 5.2 `wiring_checklist` completo sin ítems `failing` no justificados.
- [ ] 5.3 `openspec validate metricas-deteccion-patrones-inicio --strict`.
- [ ] 5.4 Back-reference: agregar `openspec_change: metricas-deteccion-patrones-inicio` en `docs/03-backlog/epicas.md#ep-004` y en el frontmatter/nota de cada una de las 6 HU (HU-026 a HU-031).
